#include <vector>

void display_misses(int misses);
void greet();
void end_game(std::string answer, std::string codeward);

// Declare functions

void display_status(std::vector <char> incorrect, std::string answer);
